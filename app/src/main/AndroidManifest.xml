<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zy.multistatepage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/jntm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RefreshStateActivity"/>
        <activity android:name=".ApiActivity" />
        <activity android:name=".MultiViewActivity" />
        <activity android:name=".MockNetActivity" />
        <activity android:name=".LottieExtActivity" />
        <activity android:name=".ViewPager2Activity" />
        <activity android:name=".MultiFragmentActivity" />
        <activity android:name=".MultiStateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>